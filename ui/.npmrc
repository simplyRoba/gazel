engine-strict=true

# TODO: Remove once layercake updates peer dep to include TS 6.
# layercake@10.0.2 declares peerDependencies typescript ^5, but we use TS 6.
# The conflict is cosmetic -- LayerCake doesn't use TS APIs at runtime.
legacy-peer-deps=true
